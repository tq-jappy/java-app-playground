spring:
  datasource:
    # MySQL 5.x を使うので com.mysql.cj.jdbc.Driver ではなく明示的に古いドライバを指定
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test
    username: root
    password: root
